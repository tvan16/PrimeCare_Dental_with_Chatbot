// static/js/sub_detail.js
document.addEventListener('DOMContentLoaded', function() {
    // Khởi tạo slider (ví dụ dùng Bootstrap Carousel)
    const carousel = new bootstrap.Carousel(document.querySelector('.service-slider'), {
      interval: 5000,
      wrap: true
    });
  });